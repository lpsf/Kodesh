#include<stdio.h>

int main() {
    int i = 0;
    int result = 1;
    int x, n;
    scanf( "%i %i", &x, &n );
    while( i < n ) {
        result *= x;
        ++i;
    }    
    printf( "%i", result );
        return 0;
}
